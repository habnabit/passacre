[tox]
envlist = py37, py38, py39, py310, py311, pypy3
; requires = tox-pyo3

[testenv]
; pyo3 = True
deps =
    black
    isort
    hypothesis
    pytest>=5.0
    pytest-benchmark
commands =
    pytest -vvvv
    black --check tests
    isort --check-only tests
; skip_install = True
